site_name: arxiv2025_auditing_unlearning
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2025_auditing_unlearning
      handlers:
        python:
          options:
            docstring_style: numpy